<Page x:Class="WpfApp1.Views1.KunderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewmodels="clr-namespace:WpfApp1.ViewModels"
      d:DataContext="{d:DesignInstance Type=viewmodels:KundVM}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="#FF1A1A1D"
      Title="KundPage">

    <StackPanel Orientation="Vertical" Margin="20">

        <TextBlock Text="KUNDER"
                   FontSize="28"
                   FontWeight="Black"
                   Foreground="#F4F1EA"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBox x:Name="SökFält"
                     Text="{Binding SökText, UpdateSourceTrigger=PropertyChanged}"
                     Width="350"
                     Height="33"
                     FontSize="16"
                     VerticalContentAlignment="Center"/>
        </StackPanel>

        <Border Background="#FF2C3E50" CornerRadius="15" Padding="15" Margin="0,10,0,0">
            <StackPanel>
                <TextBlock Text="Registrerade Kunder"
                           FontWeight="Bold"
                           FontSize="18"
                           Foreground="#F4F1EA"
                           Margin="0,0,0,15"
                           HorizontalAlignment="Center"/>

                <ListBox ItemsSource="{Binding Kunder}"
                         Background="Transparent"
                         BorderThickness="0"
                         HorizontalContentAlignment="Stretch"
                         MaxHeight="350"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#11FFFFFF" Margin="0,2,0,2" Padding="10" CornerRadius="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Namn}" 
                                                   FontSize="14" FontWeight="Bold" Foreground="#F4F1EA"/>
                                        <TextBlock Text="{Binding Telefon, StringFormat=Tfn: {0}}" 
                                                   FontSize="12" Foreground="#F4F1EA" Opacity="0.8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Email}" 
                                                   FontSize="12" Foreground="#F4F1EA"/>
                                        <TextBlock Text="{Binding Adress}" 
                                                   FontSize="11" Foreground="#F4F1EA" Opacity="0.7"/>
                                    </StackPanel>

                                    <Button Grid.Column="2" 
                                            Content="Ta bort"
                                            Background="#E74C3C" 
                                            Foreground="White"
                                            FontWeight="Bold"
                                            Padding="10,5"
                                            HorizontalAlignment="Right"
                                            Command="{Binding DataContext.TaBortKundCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="5"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>
    </StackPanel>
</Page>