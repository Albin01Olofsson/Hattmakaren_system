<Page x:Class="WpfApp1.Views1.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="LoginPage" d:DesignHeight="1080" d:DesignWidth="1920">

    <Page.Resources>
        <Style x:Key="AnimatedButton" TargetType="Button">
            <Setter Property="Background" Value="#25FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#40FFFFFF"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GlassTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1.5"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="2,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid x:Name="RootLayout">
        <Grid x:Name="BackgroundLayer">
            <Image Source="/Images/00bce0939f636f2927b42f74f38d896a.jpg" Stretch="UniformToFill">
                <Image.Effect>
                    <BlurEffect Radius="80"/>
                </Image.Effect>
            </Image>
            <Rectangle Fill="#0D0D0D" Opacity="0.6"/>
        </Grid>

        <Border Width="400" VerticalAlignment="Center" HorizontalAlignment="Center" 
                Padding="40" CornerRadius="30" 
                BorderThickness="1.5" BorderBrush="#30FFFFFF">

            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#20FFFFFF" Offset="0"/>
                    <GradientStop Color="#08FFFFFF" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>

            <Border.Effect>
                <DropShadowEffect BlurRadius="60" Color="Black" Opacity="0.5" ShadowDepth="0"/>
            </Border.Effect>

            <StackPanel>
                <Grid Width="70" Height="70" Margin="0,-75,0,20">
                    <Ellipse Fill="#15FFFFFF" Stroke="#30FFFFFF" StrokeThickness="1"/>
                    <Image Source="/Images/top-hat-transparent-background-6-removebg-preview.png" 
                           Width="45" Height="45" Stretch="Uniform">
                        <Image.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
                        </Image.Effect>
                    </Image>
                </Grid>

                <TextBlock Text="HATTMAKAREN" HorizontalAlignment="Center" FontSize="30" 
                           FontWeight="Bold" Foreground="White" Margin="0,0,0,30" 
                           FontStyle="Italic" FontFamily="Sitka Text">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.3" ShadowDepth="2"/>
                    </TextBlock.Effect>
                </TextBlock>

                <StackPanel Margin="10,0">
                    <TextBlock Text="EMAIL" FontSize="11" FontWeight="Bold" Foreground="White" Opacity="0.6" Margin="0,0,0,2" FontFamily="Sitka Text"/>
                    <TextBox Style="{StaticResource GlassTextBox}" Margin="0,0,0,20" TextChanged="TextBox_TextChanged" FontFamily="Sitka Text"/>

                    <TextBlock Text="LÖSENORD" FontSize="11" FontWeight="Bold" Foreground="White" Opacity="0.6" Margin="0,0,0,2" FontFamily="Sitka Text"/>
                    <PasswordBox x:Name="PassInput" PasswordChanged="PassInput_PasswordChanged" 
                                 Height="25" Background="Transparent" BorderBrush="#40FFFFFF" 
                                 Foreground="White" CaretBrush="White"
                                 BorderThickness="0,0,0,1.5" VerticalContentAlignment="Center"/>
                </StackPanel>

                <Button Content="Logga in" Click="Button_Click"
                        Style="{StaticResource AnimatedButton}"
                        Width="200" Height="45" Margin="0,40,0,0" FontWeight="Bold" FontSize="16"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>