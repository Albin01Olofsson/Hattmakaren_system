<Page x:Class="WpfApp1.Views1.Planneringsviews.AnvPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Views1.Planneringsviews" 
      xmlns:viewmodels="clr-namespace:WpfApp1.ViewModels" 
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AnvPage" Background="#FF1A1A1D">
    
    <Page.Resources>
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#FFC5A059" CornerRadius="0, 0, 15, 15" Panel.ZIndex="10">
            <StackPanel>
                <TextBlock Text="Ditt Schema" FontSize="28" FontWeight="Bold" Margin="30, 0"/>
                <Menu Margin="50,0" Background="#FFC5A059" FontWeight="Bold">
                    <MenuItem Header="Ordrar" Click="Ordrar_Click">

                    </MenuItem>
                    <MenuItem Header="Plannering">

                    </MenuItem>
                    <MenuItem Header="Material">

                    </MenuItem>
                    <!--<Button Content="Skapa aktivitet" Click="Button_Click"/>-->
                </Menu>
            </StackPanel>
        </Border>

        <Border Grid.Row="1">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0, 0, 0, 10">
                    <Button Content="&lt; Föregående" Command="{Binding TidigareVeckaCommand}" Background="#FFB08D46" Margin="20,0"/>
                    <TextBlock Text="{Binding DennaVeckaTxt}" VerticalAlignment="Center" Foreground="White" FontSize="18" Margin="20, 0"/>
                    <Button Content="Nästa &gt;" Command="{Binding NästaVeckaCommand}" Background="#FFB08D46" Margin="5"/>
                    <Button Content=" Månadsvy" Command="{Binding VisaMånadsVyCommand}" Margin=" 20, 5, 5, 5"/>
                </StackPanel>

                <Grid Grid.Row="1" Margin="60, 0, 0, 5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Måndag" Foreground="#FFC5A059" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <TextBlock Grid.Column="1" Text="Tisdag" Foreground="#FFC5A059" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <TextBlock Grid.Column="2" Text="Onsdag" Foreground="#FFC5A059" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <TextBlock Grid.Column="3" Text="Torsdag" Foreground="#FFC5A059" HorizontalAlignment="Center" FontWeight="Bold"/>
                    <TextBlock Grid.Column="4" Text="Fredag" Foreground="#FFC5A059" HorizontalAlignment="Center" FontWeight="Bold"/>
                </Grid>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="1" BorderBrush="#22FFFFFF" BorderThickness="1,0,0,0" Panel.ZIndex="1"/>
                        <Border Grid.Column="2" BorderBrush="#22FFFFFF" BorderThickness="1,0,0,0" Panel.ZIndex="1"/>
                        <Border Grid.Column="3" BorderBrush="#22FFFFFF" BorderThickness="1,0,0,0" Panel.ZIndex="1"/>
                        <Border Grid.Column="4" BorderBrush="#22FFFFFF" BorderThickness="1,0,0,0" Panel.ZIndex="1"/>
                        <Border Grid.Column="5" BorderBrush="#22FFFFFF" BorderThickness="1,0,1,0" Panel.ZIndex="1"/>

                        <ItemsControl ItemsSource="{Binding TidsIntervaller}" Grid.Column="0" Grid.ColumnSpan="6">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Height="60">
                                        <TextBlock Text="{Binding}" Foreground="#FFC5A059" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0, 0, 10, 0" FontSize="14"/>
                                        <Line X1="0" X2="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid}}" Y1="30" Y2="30" Stroke="#44FFFFFF" StrokeThickness="1" StrokeDashArray="4, 4" VerticalAlignment="Center" SnapsToDevicePixels="True"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <StackPanel Grid.Column="1" Panel.ZIndex="2">
                            <TextBlock Text="{Binding MåndagDatum}" Foreground="#88FFFFFF" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Panel.ZIndex="2">
                            <TextBlock Text="{Binding TisdagDatum}" Foreground="#88FFFFFF" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                        </StackPanel>
                        <StackPanel Grid.Column="3" Panel.ZIndex="2">
                            <TextBlock Text="{Binding OnsdagDatum}" Foreground="#88FFFFFF" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                        </StackPanel>
                        <StackPanel Grid.Column="4" Panel.ZIndex="2">
                            <TextBlock Text="{Binding TorsdagDatum}" Foreground="#88FFFFFF" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                        </StackPanel>
                        <StackPanel Grid.Column="5" Panel.ZIndex="2">
                            <TextBlock Text="{Binding FredagDatum}" Foreground="#88FFFFFF" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding Bokningar}" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="5">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Grid IsItemsHost="True">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition/>
                                            <ColumnDefinition/>
                                            <ColumnDefinition/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>
                                    </Grid>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Grid.Column" Value="{Binding Kolumn}"/>
                                    <Setter Property="VerticalAlignment" Value="Top"/>
                                    <Setter Property="Margin" Value="{Binding Margin}"/>
                                </Style>
                            </ItemsControl.ItemContainerStyle>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <!-- Bokningen -->
                                        <Border x:Name="BookingBorder" Background="#CCB08D46" CornerRadius="4" BorderBrush="#FFC5A059" BorderThickness="1" Height="{Binding Height}" Margin="2,0" Width="{Binding Width}">
                                            <StackPanel Margin="5">
                                                <TextBlock Text="{Binding ProduktNamn}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                                <TextBlock Text="{Binding AnvändarNamn}" Foreground="#DDFFFFFF" FontSize="11"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- Popup -->
                                        <Popup x:Name="PopupInfo" Placement="Right" PlacementTarget="{Binding ElementName=BookingBorder}" StaysOpen="True">
                                            <Border Background="#222" Padding="10" CornerRadius="6" BorderBrush="#FFC5A059" BorderThickness="1">
                                                <StackPanel Width="180">
                                                    <TextBlock Text="{Binding ProduktNamn}" Foreground="White" FontWeight="Bold" FontSize="14"/>
                                                    <TextBlock Text="{Binding AnvändarNamn}" Foreground="#DDFFFFFF"/>
                                                    <TextBlock Foreground="#AAFFFFFF">
                                                        <Run Text="Order: "/>
                                                        <Run Text="{Binding OrderId}"/>
                                                    </TextBlock>
                                                    <TextBlock Foreground="#AAFFFFFF">
                                                        <Run Text="ProduktID: "/>
                                                        <Run Text="{Binding ProduktId}"/>
                                                    </TextBlock>
                                                    <TextBlock Foreground="#AAFFFFFF" Text="{Binding StartTid, StringFormat=Start: {0:HH:mm}}"/>

                                                    <!-- Delete knapp -->
                                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                                        <Button Content="Delete" 
                                                                Command="{Binding DataContext.DeleteBokningCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                                                CommandParameter="{Binding PlaneringsId}"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </Popup>

                                        <!-- Hover logik -->
                                        <Grid.Triggers>
                                            <!-- När mus går in -->
                                            <EventTrigger RoutedEvent="MouseEnter">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <BooleanAnimationUsingKeyFrames  Storyboard.TargetName="PopupInfo" Storyboard.TargetProperty="IsOpen">
                                                            <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="True"/>
                                                        </BooleanAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                            <!-- När mus lämnar -->
                                            <EventTrigger RoutedEvent="MouseLeave">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <BooleanAnimationUsingKeyFrames Storyboard.TargetName="PopupInfo" Storyboard.TargetProperty="IsOpen">
                                                            <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="False"/>
                                                        </BooleanAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Grid.Triggers>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.RowSpan="2" Background="#CC000000" Visibility="{Binding MånadsvySynlig, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Calendar SelectedDate="{Binding ValtDatum, Mode=TwoWay}" DisplayDate="{Binding NuvarandeMåndag}"/>
                <Button Content="Stäng" Width="100" Margin="10" Command="{Binding StängMånadsvyCommand}"/>
            </StackPanel>
        </Border>
    </Grid>

</Page>



